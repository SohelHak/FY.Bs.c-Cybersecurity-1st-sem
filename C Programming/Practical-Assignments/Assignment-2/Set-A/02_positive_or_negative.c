#include <stdio.h>

int main(){
    int num;
    printf("Enter Number: ");
    scanf("%d", &Num);

    if(num >= 0){
        printf("%d is Positive Number!\n", num);
    }
    else
        printf("%d is Negative Number!\n", num);

    return 0;
}
